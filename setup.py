#!/usr/bin/env python

from setuptools import setup, find_packages

setup(name='graf_nas',
      version='0.1',
      description='GRAF features',
      packages=find_packages(),
      include_package_data=True
)

